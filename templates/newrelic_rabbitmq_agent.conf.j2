# vim:filetype=upstart
# {{ ansible_managed }}

description "New Relic RabbitMQ agent"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 3 30

exec newrelic_rabbitmq_agent -c {{newrelic_rabbitmq_agent_conf_path}}
