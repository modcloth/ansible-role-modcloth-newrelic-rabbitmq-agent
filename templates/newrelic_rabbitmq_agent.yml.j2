# {{ansible_managed}}
# vim: ft=yaml

newrelic:
  license_key: {{new_relic_license_key}}
  verbose: {{newrelic_rabbitmq_agent_verbose}}

agents:
  rabbitmq:
{% for agent in newrelic_rabbitmq_agent_agents %}
  - name: {{agent.name}}
    uri: {{agent.uri}}
{% endfor %}
